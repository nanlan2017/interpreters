(module expand-type (lib "eopl.ss" "eopl")

  (require "0-lang.scm")

  (provide expand-type)
  (provide expand-iface)

  ;; these are stubs.
  ;; They will be replaced by something more interesting in abstract-types-lang.

  (define (expand-type ty tenv)
    ty)
  (define (expand-iface m-name iface tenv)
    iface)

  )
